# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!
        
#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v1
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ap-south-1      

      # Runs a set of commands using the runners shell
      - name: fetching env
        run: |
              ${{secrets.AWS_CRED}} aws secretsmanager get-secret-value --region ap-south-1 --secret-id mobile-test | jq .SecretString | jq --raw-output . |  jq -r 'to_entries|map("\(.key)=\(.value|tostring)")|.[]' > .env
              dotenv -f .env
              source .env
              echo $API_URL
              echo $CID
              echo $REACT_APP_RAZOR_PAY_KEY
              echo $CURRENCY_SYMBOL
#       - name: Install dotenv
#         uses: michaeldyrynda/action-dotenv@v2
#         with:
#           filename: .env
      - name: Dotenv Action
        uses: falti/dotenv-action@v1.0.4
      
      - name: Environment Variables from Dotenv
        uses: c-py/action-dotenv-to-setenv@v4

      
      - name: echo
        run : |
              echo $API_URL
              echo $CID
              echo $REACT_APP_RAZOR_PAY_KEY
              echo $CURRENCY_SYMBOL
          
